spec:
  name: openbotx
  services:
    - name: web
      git:
        branch: main
        repo_clone_url: https://github.com/openbotx/openbotx.git
      dockerfile_path: Dockerfile
      http_port: 8000
      instance_count: 1
      instance_size_slug: basic-xxs
